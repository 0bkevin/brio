# Optional local overrides read by the root Makefile.
BRIO_ADDR=127.0.0.1:8787
BRIO_RELAY_ADDR=127.0.0.1:8082
BRIO_RELAY_URL=http://127.0.0.1:8082
BRIO_RELAY_ISSUER=http://127.0.0.1:8082
BRIO_AGENT_ID=local-brio
HERMES_API_BASE=http://127.0.0.1:8642

# Explicit loopback-only auth for local development. Production must not set
# either flag and must configure Clerk below instead.
BRIO_DEV_AUTH=true
EXPO_PUBLIC_BRIO_DEV_AUTH=true
EXPO_PUBLIC_BRIO_RELAY_URL=http://127.0.0.1:8082

# Optional companion settings.
# BRIO_PUBLIC_URL=http://127.0.0.1:8787
# BRIO_TOKEN=replace-with-a-local-dev-token
# HERMES_API_KEY=replace-with-hermes-api-key
# HERMES_HOME=/Users/you/.hermes
# BRIO_ALLOWED_ROOTS=/Users/you/projects,/Users/you/.hermes
# BRIO_CLOUDFLARED_PATH=cloudflared

# Optional relay persistence. If unset, the relay uses in-memory storage.
# BRIO_DATABASE_URL=postgres://user:password@localhost:5432/brio?sslmode=disable
# BRIO_RELAY_SIGNING_KEY=-----BEGIN PRIVATE KEY-----...-----END PRIVATE KEY-----

# Production identity. Create a Clerk JWT template whose exact audience is
# "brio-relay", then configure the same template name in the mobile build.
# BRIO_CLERK_SECRET_KEY=sk_live_...
# BRIO_CLERK_JWT_KEY=-----BEGIN PUBLIC KEY-----...-----END PUBLIC KEY-----
# BRIO_CLERK_ISSUER=https://your-instance.clerk.accounts.dev
# BRIO_CLERK_JWT_AUDIENCE=brio-relay
# BRIO_CLERK_AUTHORIZED_PARTIES=https://app.example.com,brio://
# EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
# EXPO_PUBLIC_CLERK_JWT_TEMPLATE=brio-relay
# EXPO_PUBLIC_BRIO_RELAY_URL=https://connect.example.com

# Optional hosted managed endpoints.
# BRIO_CLOUDFLARE_ACCOUNT_ID=
# BRIO_CLOUDFLARE_API_TOKEN=
# BRIO_CLOUDFLARE_ZONE_ID=
# BRIO_TUNNEL_BASE_DOMAIN=connect.example.com
# BRIO_MANAGED_TUNNEL_LIMIT=3
